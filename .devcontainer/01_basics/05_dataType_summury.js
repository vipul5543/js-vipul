// primitive

// 7 types:  string, number, boolean, null, undefined, symbol,BigInt
// datatype of null is object

// non primitive(reference)
// Array, objects, functions // datatype of these are object and that of functions is object function


const hero = ["vipul","atul"];

let myObj = {
    name: "vipul"
    age: 22
}

const myFunction = function(){
    console.log("hello");
}